const assert = require("chai").asssert;
const main = require("../index");

describe("The registrationn login App", function () {
    let name = tim;
    // let password = pass; 

    it("check for login credentials are true", function () {
        assert.isString(name.firstName("tim"), "string");
    })
});